# Snake RL (Minimal DQN)

A compact Deep Q-Learning implementation for Snake using PyTorch. Designed for quick experimentation.

See usage in repository root. Use CUDA 12.1-compatible PyTorch wheel if you have an NVIDIA GPU.
